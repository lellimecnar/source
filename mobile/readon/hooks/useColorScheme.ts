export { useColorScheme } from 'react-native';

// import { type ColorScheme } from '@/const/colors';

// export const useColorScheme = (): ColorScheme => {
// 	return useDefaultColorScheme() ?? 'light';
// };
